# .env.example — SAFE TO COMMIT (placeholder values only)
# Both APIs used in this project are FREE with NO KEY required.
# CoinGecko:     https://api.coingecko.com — no key
# REST Countries: https://restcountries.com — no key
# NEVER commit your actual .env file (excluded by .gitignore).
